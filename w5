#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");
    int n;
    cout << "Введите число: ";
    if (!(cin >> n))
        return 0;

    int i = 1;
    while (i <= 10) {
        cout << n << " × " << i << " = " << n * i << endl;
        ++i;
    }
    return 0;
}
