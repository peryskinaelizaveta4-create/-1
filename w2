#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");
    int n;
    cout << "Введите число: ";
    if (!(cin >> n)) return 0;

    while (n > 0) {
        cout << "Отсчет:  " << n << '\n';
        --n;
    }
    cout << '\n' << "Старт!" << endl;
    return 0;
}
